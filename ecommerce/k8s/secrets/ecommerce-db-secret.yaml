# Database credentials for MySQL + backend. Apply before StatefulSet/Deployment.
# For production, rotate values and avoid committing real secrets to a public repo.
apiVersion: v1
kind: Secret
metadata:
  name: ecommerce-db-credentials
  namespace: default
type: Opaque
stringData:
  MYSQL_ROOT_PASSWORD: "My:S3cr3t"
  DB_USER: "root"
  DB_PASSWORD: "My:S3cr3t"
  DB_NAME: "ecommerce"
